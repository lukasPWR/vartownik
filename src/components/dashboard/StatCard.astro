---
interface Props {
  value: string;
  label: string;
  accent?: string;
}

const { value, label, accent = "text-white" } = Astro.props;
---

<div
  class="flex flex-col gap-3 rounded-xl border border-white/10 bg-white/5 p-5 backdrop-blur-sm"
  role="figure"
  aria-label={label}
>
  <div class="flex items-center gap-2 text-white/60">
    <span class="size-5 shrink-0" aria-hidden="true">
      <slot name="icon" />
    </span>
    <p class="text-sm font-medium">{label}</p>
  </div>
  <p class={`text-3xl font-bold tracking-tight ${accent}`}>{value}</p>
</div>
